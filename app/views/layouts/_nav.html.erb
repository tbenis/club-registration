<% if logged_in? %>
  <nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <!-- Navbar content -->
    <div class="container">
      <%= link_to "Home", user_path(session[:user_id]), class: "nav-link" if session[:user_id].present? %>
     <!-- 
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
     -->
    </div>
    <!-- 
    <div class="collapse navbar-collapse" id="navbarNav">
    -->
      <ul class="navbar-nav">
        <li class="nav-item active">
          <%= link_to "Create a Club", new_user_club_url(session[:user_id]), class: "nav-link" if session[:user_id].present? %>
          <!-- <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a> -->
        </li>
        <li class="nav-item">
          <%= link_to "All clubs", clubs_path, class: "nav-link" if session[:user_id].present?  %>
        </li>
        <li class="nav-item">
          <%= link_to "Log Out", logout_path, method: :delete, class: "nav-link" if session[:user_id].present? %>
        </li>
      </ul>
    <!-- </div> -->
  </nav>
<% else %>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <!-- Navbar content -->
    <div class="container">
      <%= link_to "Home", '/', class: "nav-link" %>
      <!-- 
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      -->
    </div>
    <!-- 
    <div class="collapse navbar-collapse" id="navbarNav">
    -->
      <ul class="navbar-nav">
        <li class="nav-item active">
          <%= link_to "Login", login_path, class: "nav-link" %>
          <!-- <a class="nav-link" href="#">Home <span class="sr-only">(current)</span></a> -->
        </li>
        <li class="nav-item">
          <%= link_to "Signup", signup_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Sign in or Signup with google", '/auth/google_oauth2',  method: :post, class: "nav-link" %>
        </li>
      </ul>
      <!-- 
    </div>
     -->
  </nav>
<%end%>
<br>
